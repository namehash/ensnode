name: Static Analysis

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  static-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Audit dependencies
        run: cd apps/ensnode && pnpm audit --audit-level=low

      - name: Install dependencies
        run: cd apps/ensnode && pnpm install --frozen-lockfile

      - name: Run Biome CI
        run: cd apps/ensnode && pnpm biome ci

      - name: Run TypeScript type checks
        run: cd apps/ensnode && pnpm run typecheck

      - name: Run Ponder build checks
        run: cd apps/ensnode && pnpm codegen
