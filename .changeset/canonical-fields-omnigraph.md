---
"ensapi": minor
---

**Omnigraph**: expose `Domain.canonical` and `Registry.canonical` on the Omnigraph schema. Both are nullable `Boolean` fields indicating whether the entity participates in the canonical namegraph.
