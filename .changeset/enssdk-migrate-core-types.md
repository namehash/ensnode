---
"enssdk": minor
"@ensnode/ensnode-sdk": minor
---

Migrated core ENS types and utilities from `ensnode-sdk` to `enssdk`. Added typed `namehash`/`labelhash` wrappers, re-exports `Address`/`Hex` from viem, and exports `asLowerCaseAddress`.
