services:
  ensadmin:
    image: ghcr.io/namehash/ensnode/ensadmin:${ENSNODE_VERSION:-1.10.1}
    build:
      dockerfile: ./apps/ensadmin/Dockerfile
      context: ../..
    ports:
      - "4173:4173"
