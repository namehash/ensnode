---
import Default from '@astrojs/starlight/components/Header.astro';
import DeafaultSearch from "@astrojs/starlight/components/Search.astro"

const isHomepage = Astro.locals.starlightRoute.id === '';
---

{
    isHomepage ? (
    <header>Built with Starlight 🌟 <DeafaultSearch/></header>
        ) : (
    <Default>
        <slot />
    </Default>
        )
    }
