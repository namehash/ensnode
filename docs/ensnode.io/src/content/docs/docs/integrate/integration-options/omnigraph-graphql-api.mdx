---
title: ENS Omnigraph GraphQL API
description: Query the ENS Omnigraph API directly via HTTP from any language.
---

import Walkthrough from "@components/walkthroughs/omnigraph-graphql-api.mdx";

{/* This walkthrough is version-locked to the production-deployed Omnigraph schema; it is authored against */}
{/* the vendored snapshot (see `@data/omnigraph-examples/snapshot.json`). Production lags `main` and its */}
{/* schema differs, so new-version work happens on a separate branch. */}
<Walkthrough />
