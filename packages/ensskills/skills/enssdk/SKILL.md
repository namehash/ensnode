---
name: enssdk
description: Reference for integrating ENS into JavaScript/TypeScript apps with the enssdk library (typed Omnigraph client via gql.tada, hashing, normalization). Coming soon — for now the omnigraph skill covers the query model enssdk wraps.
---

# enssdk (coming soon)

A dedicated `enssdk` integration skill is planned (typed Omnigraph queries with `gql.tada`, client
setup, hashing/normalization helpers). Until then, the **omnigraph** skill covers the query model and
data shapes that `enssdk` exposes in TypeScript.
